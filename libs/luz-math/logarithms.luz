# logarithms.luz — ln, log2, log10, log(x, base)
# ln uses the atanh series: ln(x) = 2 * sum((x-1)/(x+1))^(2n+1) / (2n+1)

function log(x, base) {
    if x <= 0 { alert "log: x must be > 0" }
    if base <= o or base == 1 { alert "log: base must be > 0 and != 1" }
    return ln(x) / ln(base)
}