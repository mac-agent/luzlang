name: Release Windows

on:
  push:
    tags:
      - "v*"

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Build luz.exe
        run: pyinstaller --onefile --name luz main.py

      - name: Build ray.exe
        run: pyinstaller --onefile --name ray ray.py

      - name: Install Inno Setup
        run: choco install innosetup --yes

      - name: Compile installer
        run: |
          $version = "${{ github.ref_name }}" -replace "^v", ""
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" `
            /DAppVersion=$version `
            installer\luz_installer.iss

      - name: Rename installer
        run: |
          $version = "${{ github.ref_name }}"
          Rename-Item installer_output\luz-setup.exe "luz-$version-windows-setup.exe"

      - name: Upload Windows installer to release
        uses: softprops/action-gh-release@v2
        with:
          files: installer_output/luz-${{ github.ref_name }}-windows-setup.exe
